# TODO

- [x] Per-query execution flow visualization — when a learner runs a query, show a vertical pipeline of the logical steps (FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY → LIMIT) that apply to THAT specific query, with row counts from EXPLAIN. Displayed as a tab alongside Results in the query workspace. (DONE)
